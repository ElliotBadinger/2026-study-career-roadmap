---
// DEPRECATED: This component is being replaced by Surface.astro and FeatureCard.astro
// It is now a simple wrapper for backward compatibility during refactoring.
interface Props {
  class?: string;
  href?: string; // Will be handled by parent components
}
const { class: className } = Astro.props;
---

<div class:list={["surface-s2 p-6", className]}>
  <slot />
</div>
